name: ez_call
description: Video Calling Simplified.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3

  flutter_webrtc: ^0.2.8
  socket_io_client: ^0.9.10
  socket_io: ^0.9.4
  fluttertoast: ^7.1.1


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/illustration.png
    - assets/logo.png